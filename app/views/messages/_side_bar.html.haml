.chat-side
  .side-header
    .side-header__box
      .p.user-name
        = current_user.name
      %ul.menu
        %li.menu__new-group
          =link_to new_group_path do
            = icon('fa', 'edit', class: "menu__new-group")
        %li.menu__edit-user
          = link_to edit_user_path(current_user) do
            = icon('fas', 'cog', class: 'menu__edit-user')
  .side-groups
    - current_user.groups.each do |group|
      .side-groups__group
        = link_to '#' do
          %p.group-name
            = group.name
          %p.latest-message
            メッセージはまだありません